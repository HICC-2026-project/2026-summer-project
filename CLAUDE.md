# Git commit convention

Do not add a `Co-Authored-By: Claude` (or any AI co-author) trailer, and do not add a "Generated with Claude Code" footer, to commit messages anywhere in this repository (backend, frontend, or root). Commit as the operating user only — this keeps the GitHub contributor list accurate to actual team members.
