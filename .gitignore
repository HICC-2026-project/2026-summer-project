# Secrets (Notion 원칙: API 키는 절대 GitHub에 올리지 않는다)
.env
**/application-local.yml
**/application-secret.yml

# SSH keys (EC2 접속 키 — 절대 커밋 금지)
*.pem

# Maven build output
target/
!.mvn/wrapper/maven-wrapper.jar

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Local uploaded proof images
**/uploads/

# Claude Code 로컬 런타임 (launch.json은 팀 공유용이라 제외하지 않음)
.claude/worktrees/
.claude/scheduled_tasks.lock
.claude/settings.local.json